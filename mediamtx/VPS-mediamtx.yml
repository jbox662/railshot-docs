# Copy this ENTIRE file to C:\mediamtx\mediamtx.yml on the VPS
# OR run deploy-mediamtx-to-vps.bat from the repo root on the VPS
#
# Important: use mediamtx_v1.19.2_windows_amd64\mediamtx.yml (full config)
# This stub is kept for reference only — table1 settings:

paths:
  table1:
    source: rtsp://admin:decoder1@140.106.76.67:8554/h264Preview_01_main
    sourceOnDemand: yes
    sourceOnDemandStartTimeout: 30s
    sourceOnDemandCloseAfter: 15s
    rtspTransport: tcp
